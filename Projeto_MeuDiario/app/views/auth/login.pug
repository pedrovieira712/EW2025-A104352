extends ../layout

block content
  .row.justify-content-center.mt-5
    .col-md-4
      .card.shadow
        .card-body.p-4
          .text-center.mb-4
            i.bi.bi-journal-text.text-primary(style='font-size: 3rem;')
            h3.mt-2 Login
          
          if error
            .alert.alert-danger= error
          
          form(method='POST', action='/auth/login')
            .mb-3
              input.form-control.form-control-lg(
                type='text', 
                name='username', 
                placeholder='Username',
                required
              )
            
            .mb-3
              input.form-control.form-control-lg(
                type='password', 
                name='password', 
                placeholder='Password',
                required
              )
            
            .d-grid.mb-3
              button.btn.btn-primary.btn-lg(type='submit') Entrar
          
          .text-center.mb-3
            p.text-muted Ou entre com:
          
          .d-grid.gap-2
            a.btn.btn-danger.btn-lg(href='/auth/google')
              i.bi.bi-google.me-2
              | Entrar com Google
          
          .text-center.mt-3
            | Ainda sem conta? 
            a(href='/auth/register') Criar conta
